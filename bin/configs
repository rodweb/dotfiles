#!/usr/bin/env bash
pushd "$HOME" || exit
chosen=$(yadm ls-files | dmenu -p "edit:")
[[ -z "$chosen" ]] && exit
if [[ "$TERM" =~ "tmux" ]]; then
  $EDITOR "$HOME/$chosen"
  popd || exit
  exit
fi
popd || exit
sensible-terminal -e zsh -i -c "$EDITOR $HOME/$chosen"
